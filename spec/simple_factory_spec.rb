require 'spec_helper'

describe SimpleFactory do
  it 'has a version number' do
    expect(SimpleFactory::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
